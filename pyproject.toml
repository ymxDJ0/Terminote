[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Terminote"
version = "0.1.0"
description = "A terminal note-taking app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
terminote = "main:main"